<Window x:Class="AppParrent.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppParrent"
        mc:Ignorable="d" WindowStyle="None"
        Title="MainWindow"  ResizeMode="CanResizeWithGrip" Height="680" Width="600" AllowsTransparency="True">
    <Window.Background>
        <SolidColorBrush Opacity="0" Color="White"/>
    </Window.Background>
    <Border BorderBrush="Gray" BorderThickness="1">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="Open" Click="btnRun_Click" />
            <MenuItem Header="Play" Click="play_Click" />
            <MenuItem Header="Stop" Click="stop_Click"/>
        </Menu>
        <!--<Canvas Grid.Row="1" Background="Transparent" Opacity="0.1"/>-->
    </Grid>
    </Border>
</Window>
